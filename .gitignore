# Python (capcut-mcp, media-analysis) — each keeps its own venv
.venv/
__pycache__/
*.pyc
*.egg-info/
.uv/

# Node / TypeScript (premiere-*)
node_modules/
dist/
*.tsbuildinfo

# Build/test artifacts
build/
tests/assets/
*.log
.DS_Store

# Scratch workspace: extracted frames, audio slices, transcripts, one-off build scripts.
.frames/

# Local MCP config holds API keys (Pexels/Pixabay) — keep it out of git.
# Share the sanitized template instead: .mcp.json.example
.mcp.json

# Claude Code local state
.claude/scheduled_tasks.lock
